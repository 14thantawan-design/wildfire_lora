PORT=4000
MONGODB_URI=mongodb://127.0.0.1:27017/wildfire_lora

# Must match GATEWAY_API_KEY in gateway/secrets.h.
GATEWAY_API_KEY=replace-with-a-long-random-key

# Browser origins allowed to call the API directly.
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://wildfire.nattaphat.me,https://admin.nattaphat.me

# Cloudflare Access protects this administrator hostname. The backend validates
# Access JWT signatures; manage administrator emails only in the Access policy.
ADMIN_HOSTNAME=admin.nattaphat.me
CF_ACCESS_TEAM_DOMAIN=https://your-team-name.cloudflareaccess.com
CF_ACCESS_AUD=replace-with-the-application-audience-tag

GATEWAY_OFFLINE_TIMEOUT_MS=30000

# TEST_MODE default: mark nodes offline if last_seen is older than 60 seconds.
OFFLINE_TIMEOUT_MS=60000
# Allow one expected report to be missed before a node is shown as offline.
OFFLINE_INTERVAL_MULTIPLIER=2.5
OFFLINE_JITTER_GRACE_MS=30000

# Telegram Channel notifications. Leave both blank to keep notifications disabled.
# For a public channel, TELEGRAM_CHAT_ID can be its username, for example @WildfireLoraAlerts.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_DASHBOARD_URL=https://wildfire.nattaphat.me
TELEGRAM_TIMEZONE=Asia/Bangkok
